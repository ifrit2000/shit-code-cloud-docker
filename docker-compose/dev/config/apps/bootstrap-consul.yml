spring:
  cloud:
    consul:
      port: 8500
      config:
        format: FILES
        prefix: config
        #        prefix: config/${spring.profiles.active}
        watch:
          enabled: true
          delay: 1
        enabled: true

